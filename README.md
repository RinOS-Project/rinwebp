RinOS local WebP implementation.

This directory contains the in-tree WebP API subset used by RinBrowser:

- `WebPGetInfo`
- `WebPDecodeBGRAInto`

Current implementation is self-contained and does not depend on V8's packaged
libraries at runtime.

